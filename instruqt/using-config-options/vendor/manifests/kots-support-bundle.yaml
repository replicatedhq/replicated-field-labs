apiVersion: troubleshoot.sh/v1beta2
kind: SupportBundle
metadata:
  name: sts-app
spec:
  collectors:
    - logs:
        name: sts-app-logs
        selector:
          - app=sts-app
        namespace: '{{repl Namespace }}'
  analyzers: []

