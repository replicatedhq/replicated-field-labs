usr/bin/env bash

# This set line ensures that all failures will cause the script to error and exit
set -euxo pipefail
 
agent variable set SLACKERNEWS_DOMAIN slackernews.${HOSTNAME}.${_SANDBOX_ID}.instruqt.io

